# based

The package manager for Project Based.

## TODOs

- [x] `based-build`: Barebones package builder
- [x] `based-install`: Barebones package installation
- [ ] `based-remove`: Barebones package removal
- [ ] `based-install`: Remote package repository support
- [ ] `based-*`: Dependency resolution
- [ ] `based-build`: Split package support

## Credits

The layout of this project was inspired by `distrobox`.
